PROPERTIES

.length

METHOD tanpa deklarasi variabel temporer

old.charAt(indeks)
old.concat(string)
old.indexOf(string/karakter)



METHOD dengan deklarasi variabel temporer

var new = old.substring(indeks awal, akhir-opsional)
var new = old.substr(indeks awal, jml karakter-opsional)
var new = old.toUpperCase()
var new = old.toLowerCase()
var new = old.trim()

TYPECASTING

var new = String(old)
var new = old.toString()
var new = Number(var/'string')
var new = parseInt(old)
var new = parseFloat(old)
